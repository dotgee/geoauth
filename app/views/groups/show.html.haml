%p#notice= notice

%p
  %b Name:
  = @group.name
%p
  %b Description:
  = @group.description

- if @group.members.any?
  = render partial: 'shared/users_list', locals: { users: @group.members }
= link_to 'Edit', edit_group_path(@group)
\|
= link_to 'Back', groups_path
